version: '2'
services:
  searchingservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/searchingService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=searchingService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
